MoneyMate V2.6 Final Stable

Fixed in this build:
- Service worker cache updated to V2.6.0 and old caches removed on activation.
- Service worker update bypasses browser HTTP cache.
- IndexedDB restore now completes before the first render/save, preventing saved transactions from being overwritten with empty data on refresh.
- App title, header and manifest updated to V2.6 Final Stable.

Data is stored locally in the browser. Use Settings > Backup regularly.
